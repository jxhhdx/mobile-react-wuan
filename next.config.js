const resolve = require('path').resolve
module.exports = {
    /* config options here */
    webpack: (config, { buildId, dev, isServer, defaultLoaders, webpack }) => {

        return config
    },
}